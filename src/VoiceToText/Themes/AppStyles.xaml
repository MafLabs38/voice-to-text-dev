<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Pas de style TargetType="Window" ici : chaque fenêtre thémée fixe directement son
        Background/Foreground en DynamicResource vers Theme.* (résolu dans Application.Resources,
        posé par ThemeManager). Une tentative précédente passait par un style implicite déclaré
        dans les Resources de la fenêtre elle-même — ça ne s'applique jamais à la fenêtre qui
        possède ce dictionnaire, seulement à ses enfants, d'où un fond resté blanc malgré un
        thème sombre sélectionné.
    -->

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}" />
    </Style>

    <!--
        Même limite que ComboBox/ComboBoxItem : la case/le rond par défaut sont peints par le
        chrome système (blanc), pas par une propriété stylable — repéré par l'audit (case blanche
        vive sur fond sombre). Template complet pour un rendu cohérent avec le thème.
    -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}" />
        <Setter Property="Background" Value="{DynamicResource Theme.Background}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="Box" Width="16" Height="16" Margin="0,0,8,0"
                                Background="{DynamicResource Theme.Surface}"
                                BorderBrush="{DynamicResource Theme.Border}" BorderThickness="1" CornerRadius="3">
                            <Path x:Name="Check" Data="M2,7 L6,11 L14,2" Stroke="{DynamicResource Theme.OnPrimary}"
                                  StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round" Visibility="Collapsed" />
                        </Border>
                        <ContentPresenter VerticalAlignment="Center" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="{DynamicResource Theme.Primary}" />
                            <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}" />
        <Setter Property="Background" Value="{DynamicResource Theme.Background}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <StackPanel Orientation="Horizontal">
                        <Border x:Name="Circle" Width="16" Height="16" Margin="0,0,8,0"
                                Background="{DynamicResource Theme.Surface}"
                                BorderBrush="{DynamicResource Theme.Border}" BorderThickness="1" CornerRadius="8">
                            <Ellipse x:Name="Dot" Width="8" Height="8" Fill="{DynamicResource Theme.OnPrimary}"
                                     Visibility="Collapsed" />
                        </Border>
                        <ContentPresenter VerticalAlignment="Center" />
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Circle" Property="Background" Value="{DynamicResource Theme.Primary}" />
                            <Setter TargetName="Dot" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource Theme.Surface}" />
        <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="CaretBrush" Value="{DynamicResource Theme.TextPrimary}" />
    </Style>

    <Style TargetType="PasswordBox">
        <Setter Property="Background" Value="{DynamicResource Theme.Surface}" />
        <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="CaretBrush" Value="{DynamicResource Theme.TextPrimary}" />
    </Style>

    <!--
        ComboBox par défaut : sa "boîte fermée" est un ToggleButton avec son propre chrome système
        qui ignore Background/Foreground sans template complet — d'où les 3 champs illisibles
        (texte clair sur fond clair) dans la première version.
    -->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{DynamicResource Theme.Surface}" />
        <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Border}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="6,4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press" Focusable="False">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="{DynamicResource Theme.Surface}"
                                            BorderBrush="{DynamicResource Theme.Border}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition />
                                                <ColumnDefinition Width="18" />
                                            </Grid.ColumnDefinitions>
                                            <Path Grid.Column="1" Data="M0,0 L4,4 L8,0 Z"
                                                  Fill="{DynamicResource Theme.TextSecondary}"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Left" VerticalAlignment="Center" />
                        <Popup x:Name="Popup" IsOpen="{TemplateBinding IsDropDownOpen}"
                               Placement="Bottom" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                            <Border Background="{DynamicResource Theme.Surface}"
                                    BorderBrush="{DynamicResource Theme.Border}" BorderThickness="1"
                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                    MaxHeight="220">
                                <ScrollViewer>
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--
        Comme ComboBox, ComboBoxItem a son propre visuel de sélection intégré au template par
        défaut (un bleu système translucide) qui ignore un simple Setter sur Background — vérifié
        par l'audit : l'item sélectionné restait teinté de bleu système même avec IsSelected stylé.
        Template complet nécessaire pour vraiment reprendre la main.
    -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Padding" Value="6,4" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bg" Background="{DynamicResource Theme.Surface}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{DynamicResource Theme.Primary}" />
                            <Setter Property="TextElement.Foreground" Value="{DynamicResource Theme.OnPrimary}" />
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{DynamicResource Theme.Primary}" />
                            <Setter Property="TextElement.Foreground" Value="{DynamicResource Theme.OnPrimary}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Foreground" Value="{DynamicResource Theme.TextPrimary}" />
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource Theme.Primary}" />
        <Setter Property="Foreground" Value="{DynamicResource Theme.OnPrimary}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="4"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.85" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Slider">
        <Setter Property="Foreground" Value="{DynamicResource Theme.Primary}" />
    </Style>

</ResourceDictionary>
